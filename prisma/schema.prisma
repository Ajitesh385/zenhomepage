datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Note {
  id        String   @id @default(cuid())
  title     String   @default("Untitled")
  content   String   @default("")
  isPinned  Boolean  @default(false)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Shortcut {
  id         String   @id @default(cuid())
  title      String
  url        String
  icon       String   @default("globe")
  orderIndex Int      @default(0)
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
}

model UserSettings {
  id             String   @id @default("default")
  theme          String   @default("midnight") // midnight | obsidian | aurora | dusk | light
  zenPreference  String   @default("black")    // black | white
  searchEngine   String   @default("google")   // google | duckduckgo | perplexity | youtube | github
  wallpaper      String   @default("aurora")   // aurora | gradient | abstract | clean | custom
  customBgUrl    String   @default("")
  showClock      Boolean  @default(true)
  showShortcuts  Boolean  @default(false)
  clockFormat    String   @default("24h")      // 12h | 24h
  clockStyle     String   @default("minimal")  // minimal | glow | cyber | serif
  showSeconds    Boolean  @default(false)
  birthDate      String   @default("2003-11-15T17:05:00")
  noteFont       String   @default("sans")     // sans | mono | serif
  noteFontSize   String   @default("base")     // sm | base | lg | xl
  noteColor      String   @default("default")  // default | amber | emerald | sky | purple | rose
  calendarView   String   @default("days")     // days | year | weeks
  updatedAt      DateTime @updatedAt
}
